# ==============================================================================
# BeanFlow Payroll Backend Configuration
# ==============================================================================

# Application
APP_NAME=BeanFlow Payroll Backend
DEBUG=true
LOG_LEVEL=DEBUG

# URLs
VITE_API_URL=http://localhost:8040
VITE_FRONTEND_URL=http://localhost:5175

# CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:5175,http://localhost:3000

# ==============================================================================
# Supabase Configuration
# ==============================================================================
# Get these from your Supabase project dashboard: Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key
# JWT Secret: Settings > API > JWT Settings > JWT Secret
SUPABASE_JWT_SECRET=your-jwt-secret

# ==============================================================================
# Encryption Key (for SIN encryption in Phase 1+)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ==============================================================================
ENCRYPTION_KEY=your-fernet-encryption-key
